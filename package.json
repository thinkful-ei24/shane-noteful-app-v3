{
	"name": "noteful-app",
	"version": "0.0.1",
	"license": "MIT",
	"author": "Chris Klanac",
	"repository": {
		"type": "git"
	},
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"test": "nyc --reporter=lcov --reporter=text-summary cross-env NODE_ENV=test mocha --timeout 5000",
		"seed": "node ./utils/seed-database.js",
		"cover": "nyc --reporter=lcov --reporter=text-summary npm test"
	},
	"dependencies": {
		"express": "^4.16.3",
		"mongoose": "^5.2.8",
		"morgan": "^1.9.0"
	},
	"devDependencies": {
		"chai": "^4.1.2",
		"chai-http": "^4.2.0",
		"cross-env": "^5.2.0",
		"mocha": "^5.2.0",
		"nyc": "^13.0.1",
		"sinon": "^5.1.1"
	}
}
